# 实验分析：不同拓扑与进程分配的仿真耗时

如图5所示，对不同拓扑与进程分配的仿真耗时进行测试。数据使用deepseek流量为负载，切分后得到4个任务，分别使用不同进程数仿真。实验结果表明，仿真耗时主要受拓扑规模和进程分配策略两个因素影响。

在相同进程分配方案下，仿真耗时随拓扑规模显著增长，拓扑规模翻倍时仿真耗时增长约2-3倍，说明仿真复杂度随拓扑规模非线性增长，这主要因为大规模拓扑的路由计算、事件调度等操作复杂度更高，节点间通信路径增多导致事件处理时间成倍增加。

在相同拓扑规模下，增加进程数能够显著降低仿真耗时，从1进程增加到2进程能够获得显著的性能提升，加速比约为1.6-1.7倍，但从2进程增加到4进程的性能提升有限，加速比仅为1.1-1.2倍，说明并行效率随进程数增加而递减。这主要因为进程间通信开销和同步等待时间随进程数增加而增长，当通信开销的增长速度超过并行计算带来的性能提升时，整体性能提升就会受限。值得注意的是，在128节点拓扑下，4进程方案的耗时（96秒）略高于2进程方案（88秒），这可能是因为小规模拓扑下计算负载相对较轻，进程间通信开销和同步等待时间超过了并行计算带来的性能提升，在小规模拓扑下并行开销相对更明显。随着拓扑规模增大，4进程方案相比2进程方案的性能优势逐渐显现，256节点和512节点拓扑下4进程方案均快于2进程方案，说明大规模拓扑下计算负载更重，并行计算的优势能够更好地抵消通信开销，并行效率更高。

实验结果表明，仿真耗时与拓扑规模呈非线性增长关系，拓扑节点数翻倍，仿真耗时增长约2-3倍，这主要因为大规模拓扑的路由计算、事件调度等操作复杂度更高。仿真耗时与进程数呈递减关系，但递减幅度随进程数增加而减小，1进程到2进程的性能提升显著，2进程到4进程的性能提升有限，说明并行效率存在上限，当进程数增加到一定程度时，通信开销和同步等待时间会成为性能瓶颈。这些发现为大规模网络仿真的性能优化提供了重要参考：在拓扑规模较小时，2进程配置可能是最佳选择，既能获得显著的性能提升，又能避免过多的通信开销；在拓扑规模较大时，4进程配置能够获得更好的性能，但需要权衡内存开销和性能提升，选择合适的进程分配策略。

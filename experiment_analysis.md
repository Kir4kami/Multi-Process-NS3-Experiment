# 实验分析：不同拓扑与进程分配的内存开销

如图4所示，对不同规模的拓扑在不同进程分配方案下的内存开销进行测试。实验结果表明，内存开销主要受拓扑规模和进程分配策略两个因素影响。在相同进程分配方案下，内存开销随拓扑规模线性增长，128节点、256节点、512节点的完整通信树方案内存开销分别约为100MB、220MB和450MB，切分1进程方案分别约为350MB、387MB和526MB，切分2进程方案分别约为620MB、706MB和915MB，切分4进程方案分别约为1200MB、1350MB和1703MB，拓扑规模翻倍时内存开销也大致翻倍。在相同拓扑规模下，内存开销随进程数增加而显著增长，256节点拓扑下1进程、2进程、4进程的内存开销比例约为1:1.82:3.48，接近线性关系。值得注意的是，完整通信树方案的内存开销显著低于切分方案，以256节点拓扑为例，完整通信树方案（220MB）仅为切分1进程方案（387MB）的57%，这主要因为完整方案中所有operate共享同一套拓扑结构、路由表和全局状态，避免了重复存储，同时通过限制最大并发operate数量来控制内存峰值，而切分方案中4个子任务独立运行，各自维护完整的数据结构。随着拓扑规模增大，完整通信树方案与切分方案的差距逐渐缩小，在128节点时完整方案仅为切分1进程的29%，在512节点时为86%，说明大规模拓扑下共享数据结构的优势相对减弱。实验结果表明，内存开销与拓扑规模和进程数均呈线性关系，完整通信树方案通过共享数据结构和并发控制，内存效率更高，在内存资源有限的情况下应优先考虑该方案，在需要并行加速的场景下需要权衡内存开销与性能提升。
